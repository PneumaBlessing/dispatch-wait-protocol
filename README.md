# dispatch-wait-protocol
RFC 001: Dispatch-Wait Protocol. Native await for LLMs. Models suspend &amp; freeze KV cache, enabling zero-token waiting. Kills the polling tax, prevents hallucination loops, &amp; makes multi-agent systems deterministic. The missing async primitive for the Agentic OS.
